name: No-op CI

on:
  pull_request:
  push:
    branches:
      - main
      - dev

permissions:
  contents: read

jobs:
  noop:
    name: No-op
    runs-on: ubuntu-latest
    steps:
      - name: Pass
        run: true
