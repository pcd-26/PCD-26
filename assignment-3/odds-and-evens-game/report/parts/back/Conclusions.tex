\section{Conclusions}

The final architecture satisfies the assignment constraints with a minimal message-passing design.
The round coordinator acts as the synchronization barrier, so the implementation remains deterministic at the round
level while still exploiting concurrency inside each round.
The result is a compact tournament engine that is easy to explain during the oral discussion and stable enough for
repeatable validation.
