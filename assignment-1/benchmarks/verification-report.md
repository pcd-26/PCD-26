# Benchmark Verification Report

This file is a legacy note from the earlier benchmark layout.

The current benchmark workflow is local-only and documented in:

- `assignment-1/scripts/README.md`
- `assignment-1/README.md`
- `assignment-1/docs/benchmarking.md`

Use `python scripts/run_benchmarks.py` to generate the current result and
chart set. The chart directory is refreshed on each run so it keeps only the
latest version.
