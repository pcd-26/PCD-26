/**
 * Application entry points for Poool.
 *
 * <p>This package contains the playable launchers for the sequential,
 * platform-thread, and task-based variants. The launchers assemble the view,
 * the runtime, and the initial board configuration, but they do not own the
 * physics rules themselves.
 */
package pcd.poool;
