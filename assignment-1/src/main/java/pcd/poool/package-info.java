/**
 * Application entry points for Poool.
 *
 * <p>This package contains the playable launchers for the sequential,
 * platform-thread, and task-based variants. Each launcher wires together the
 * same view layer with a different execution strategy, which makes the code
 * useful both as a playable app and as a comparison point during the oral.
 *
 * <p>The launchers assemble the view, the runtime, and the initial board
 * configuration, but they do not own the physics rules themselves.
 */
package pcd.poool;
