/**
 * Sketch-derived concurrency helpers.
 *
 * <p>This package contains the bounded-buffer abstraction reused as a
 * reference implementation of the monitor pattern.
 */
package pcd.poool.model.concurrent;
