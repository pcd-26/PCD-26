/**
 * Game-rule layer for Poool.
 *
 * <p>This package owns scores, match lifecycle, winner calculation, and the
 * immutable snapshots exposed to the rest of the application. It consumes the
 * physics board but does not implement the low-level movement or collision
 * math.
 */
package pcd.poool.model.game;
