/**
 * Physics layer for Poool.
 *
 * <p>The physics packages contain the mutable board, the shared stepping
 * abstraction, the concrete sequential and concurrent engines, and the board
 * configurations used by tests and benchmarks.
 */
package pcd.poool.model.physics;
