/**
 * Deterministic board configurations.
 *
 * <p>The configuration classes build reproducible initial layouts with
 * different scales. They are used by the launchers, tests, and benchmarks.
 */
package pcd.poool.model.physics.config;
