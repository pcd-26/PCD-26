/**
 * Executor-based physics implementation.
 *
 * <p>This package contains the task-based engine that uses a fixed executor
 * pool to parallelize selected physics phases while keeping the commit phase
 * serialized.
 */
package pcd.poool.model.physics.taskbased;
