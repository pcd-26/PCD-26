/**
 * Shared domain model for Poool.
 *
 * <p>The model package groups the immutable math helpers, the game rules, and
 * the mutable physics board. The important separation is that game rules and
 * simulation state are shared across the different execution strategies.
 */
package pcd.poool.model;
