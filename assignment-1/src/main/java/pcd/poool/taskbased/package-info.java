/**
 * Task-based runtime and its launcher-facing adapter classes.
 *
 * <p>This package contains the executor-backed runner, its snapshot type, and
 * the thin wrappers that expose the shared runtime support under the public
 * task-based API.
 */
package pcd.poool.taskbased;
