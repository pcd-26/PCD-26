/**
 * Sketch-derived active-controller helpers.
 *
 * <p>This package keeps the reusable command/controller abstractions that
 * inspired the final runtime implementation. They are useful to understand
 * the monitor-based coordination style used elsewhere in the assignment.
 */
package pcd.poool.controller;
