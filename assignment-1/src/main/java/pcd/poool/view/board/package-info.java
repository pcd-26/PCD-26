/**
 * Swing board view for Poool.
 *
 * <p>This package contains the concrete board frame, view model, and facade
 * used by the launchers. It renders snapshots and translates user gestures
 * into callbacks, but it does not own the model state.
 */
package pcd.poool.view.board;
